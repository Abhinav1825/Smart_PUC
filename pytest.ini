[pytest]
# Disable the web3 pytest-ethereum plugin which fails due to an
# incompatible eth_typing import (ContractName removed in eth_typing>=4).
# See: https://github.com/ethereum/web3.py/issues/3263
addopts = -p no:ethereum
testpaths = tests
